---
title: Streaming
description: Inspect streamed model output as it arrives.
---

Streaming is exposed through the model and message events.

Use `onModelInvocation()` when you need chunk-level control. Use `onAiMessage()` when you want to observe or rewrite the assistant message shape over time.

Detailed streaming examples are being written.