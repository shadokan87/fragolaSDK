---
title: Guardrails
description: Install the external guardrail hook package and attach it with agent.use().
---

Install the guardrail hook package with:

```bash
npm install @fragola-ai/agent @fragola-ai/hook-guardrail
```

This page will cover:

- how to install the package,
- how to attach it with `agent.use()`,
- and how to customize rejection or moderation behavior.

For now, treat this section as reserved.