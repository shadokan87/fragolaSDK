---
title: Overview
description: Use stores for mutable runtime data that should live beside your agent logic.
---

Stores are the mutable state layer separate from `state.messages`.

Use them for counters, cached values, and runtime coordination that does not belong inside the conversation transcript.