---
title: Overview
description: Define typed functions the model can call during a step.
---

Tools connect the model to your code.

In Fragola, a tool is a named function with a description, an input schema, and a handler. The model chooses when to call it, and Fragola validates the arguments before your handler runs.