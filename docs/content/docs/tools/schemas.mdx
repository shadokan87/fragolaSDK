---
title: Schemas
description: Use Zod for built-in validation or a JSON schema string for custom validation flows.
---

Fragola supports two common schema styles:

- **Zod** for runtime validation before the handler executes.
- **JSON schema string** when you want to own validation inside the handler or mirror an existing schema source.

Zod is the shortest path for most applications.