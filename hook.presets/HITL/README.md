# hook-__PRESET_NAME__

This package is a boilerplate hook preset for Fragola agents. Replace `__PRESET_NAME__` with your preset name when creating a new preset.

Usage example:

```js
import hook from "@fragola-ai/hook-__PRESET_NAME__";

const preset = hook({ debug: true });
```

Build:

```bash
bun run build
```
