{
  "name": "test-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A test MCP server (remote HTTP transport) running on Bun with three tools: list_clients, add_client, remove_client.",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --hot src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
