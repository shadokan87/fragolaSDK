// Auto-generated from Card.json
export const Card = {
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "child": { "type": "string", "description": "The ID of the component to be rendered inside the card." }
  },
  "required": ["child"]
} as const;
export type Card = typeof Card;
