- [x] fix modelInvocation event typing: checking kind must resolve the data type between chunk and completion.
- [x] check events cover all reasonable use cases for an agent lifecycle.
- [x] jsdoc is clear and up to date
- [x] move MCP hook integrations out of the core package for now
[x] remove filesystem persistence hook from the core package
- [x] make sure after:step callback params implemented properly in agent.ts
- [x] move hook integrations out of the core package; hooks will be available as a separate npm package

## event todos
- [x] add dedicated tests for `onUserMessage`
- [x] add dedicated tests for `onAfterStateUpdate`
- [x] decide `onModelInvocation` completion contract:
  - implement non-stream `completion` emission
  - or remove `completion` from the public type/docs if only streamed `chunk` is supported
- [x] decide whether `agentCreated` belongs in lifecycle coverage and, if yes, add tests/docs for it